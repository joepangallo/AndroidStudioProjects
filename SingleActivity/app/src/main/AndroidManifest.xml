<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — declares the app's components to the Android system.
    This app has a SINGLE activity (MainActivity), registered below. Every Activity
    must be listed here or it cannot be started.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SingleActivity">

        <!--
            The app's only Activity. The intent-filter with MAIN + LAUNCHER marks it
            as the entry point — the screen that opens when the user taps the app icon.
            android:exported="true" is required for the launcher activity.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SingleActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
