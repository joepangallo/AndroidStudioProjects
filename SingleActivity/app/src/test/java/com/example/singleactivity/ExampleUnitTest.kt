package com.example.singleactivity

import org.junit.Test

import org.junit.Assert.*

/**
 * Example LOCAL unit test — runs on the development machine's JVM (no device/emulator),
 * so it's fast and good for testing pure Kotlin logic.
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        // A trivial sanity check: assert that 2 + 2 equals 4.
        assertEquals(4, 2 + 2)
    }
}
